mkdir -p ~/.mfa

complete -W "$(ls -1 ~/.mfa/*  | awk -F "/" '{print $NF}')" mfa mfa_token

function mfa () {
    mfa_token "$1" | pbcopy
}

function mfa_token () {
    oathtool --base32 --totp "$(cat ~/.mfa/$1)"
}
